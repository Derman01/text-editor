import { authApi as auth } from './model/auth';
import { documentsApi as documents } from './model/documents';
import { templatesApi as templates } from './model/templates';

export const api = {
    auth,
    documents,
    templates,
};
